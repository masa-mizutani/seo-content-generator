# アプリケーション設定
# SECRET_KEYが設定されていない場合、自動的にランダム生成されます
# 本番環境では以下のコマンドで生成したキーを使用してください：
# python -c "from app.core.security import generate_secret_key; print(generate_secret_key())"
SECRET_KEY=
ENVIRONMENT=development
DEBUG=true

# OpenAI API設定
# 実際のAPIキーは.envファイルに設定してください
OPENAI_API_KEY=your_openai_api_key_here

# WordPress API設定（オプション）
WP_API_URL=
WP_USERNAME=
WP_APP_PASSWORD=

# データベース設定
# Railway.appでは自動的に設定されます
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/railway

# スクレイピング設定
MAX_SCRAPE_PAGES=5
SCRAPE_DELAY=5  # seconds between requests
